jQuery.sap.declare("util.CommonKey");

/**
 * Holds common keys applicable to all of SAP ME
 */
util.CommonKey = {

     /**
      * ModelKey that hold the current site the user is operating on.
      */
      CurrentSite: "CurrentSite"
};
